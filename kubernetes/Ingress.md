- worker-processes: auto is set by default. To change, add the required value to ingress-nginx-controller configmap.
- sendfile: "off"
- upstream zones are only shared by the workers inside the pod, and the zone size values are generated by the nginx-controller
- no seeting for max_conn
- queues are not exposed, all these issues are managed by autoscaling
- upstream-keepalive-connections: "320", per-worker basis
- pathType: ImplementationSpecific(regex), Prefix, Exact
